import React from 'react';
import {Navbar} from 'react-bootstrap';

const HeaderNav = () => (
  <React.Fragment>
    <Navbar.Collapse id="header-nav-left">
    </Navbar.Collapse>
  </React.Fragment>
);

HeaderNav.propTypes = {
};

export default HeaderNav;
